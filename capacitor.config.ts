import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juba.safetycheck',
  appName: 'SafetyCheck Pro',
  webDir: 'dist'
};

export default config;
